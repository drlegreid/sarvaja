[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    integration: Integration tests requiring running services
    unit: Unit tests that don't require services
    slow: Slow tests (model inference)
filterwarnings =
    ignore::DeprecationWarning
